// App parameters — Replit-compatible replacement for Base44 app-params
export const appParams = {
  appId: 'elevate-education-hub',
  token: null,
  fromUrl: typeof window !== 'undefined' ? window.location.href : '/',
  functionsVersion: '1',
  appBaseUrl: '',
};
