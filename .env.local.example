# Copy this file to .env.local and fill in your values.
# Never commit .env.local — it is listed in .gitignore.

# ── Required ───────────────────────────────────────────────────────────────────
DATABASE_URL=          # Neon PostgreSQL connection string
ADMIN_TOKEN=           # Secret token for admin bootstrap
ADMIN_PASSWORD=        # Initial admin account password
JWT_SECRET=            # Random 32+ character string
SESSION_SECRET=        # Random 32+ character string
OPENAI_API_KEY=        # platform.openai.com — server-side only, never prefix with VITE_

# ── Optional: OpenAI model config (defaults shown) ────────────────────────────
# FAST  → support enhancement, assessments, exit tickets, worksheets
# QUALITY → full lesson enhancement (uses more tokens, costs more)
OPENAI_MODEL_FAST=gpt-4o-mini
OPENAI_MODEL_QUALITY=gpt-4o

# ── Optional: Billing (Stripe) ─────────────────────────────────────────────────
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=

# ── Optional: Email (SendGrid) ─────────────────────────────────────────────────
SENDGRID_API_KEY=
FROM_EMAIL=

# ── Optional: App URL (invite emails, Stripe redirect URLs) ───────────────────
APP_URL=https://your-app.vercel.app
